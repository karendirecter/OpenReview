# REFLECTION.md

## 项目反思报告

这个项目让我最直接地感受到，agentic software engineering 并不等同于把题目交给 AI 再等待结果。对 AI 时代工程师而言，更核心的能力仍然是组织工程判断、约束条件、验证机制和回滚路径。项目质量最终取决于能否把需求说明清楚、把任务拆稳、把验证条件钉死，并在 AI 走偏时及时纠偏。Superpowers 对我最大的价值，主要体现在它迫使我把工程思考显式化。

### 哪些 Superpowers 技能在你的项目里发挥了最大作用？哪些感觉形式大于实质？
  
在所有 skill 里，对我帮助较大的首先是 `brainstorming` 和 `writing-plans`。前者逼我把做一个 GitHub PR 自动评审器这种模糊想法收缩成可执行的问题定义、用户故事和模块边界；后者再把这些定义进一步压缩成具体 task，使我能以 worktree 和 subagent 为单位推进实现。但影响最大的还是 `test-driven-development`。因为评审系统涉及 webhook、diff 解析、LLM 结构化输出、队列恢复和 GitHub 评论渲染，任何一个环节有偏差，最后暴露出来的症状都可能离根因很远。如果没有先写失败测试，我很容易在看起来大致可用的状态下被假象误导。另外，它也让我真正接触并理解了 TDD 这种开发范式，下一部分会展开说明。

如果说哪些 skill 带有形式大于实质的一面，我觉得倒没有，或者说基本对开发者透明，使其无感，只要是显式体现的我认为都比较有意义。但是不知道是我项目的特殊性还是本身skill的问题，`test-driven-development`中test的编写会比较保守，即以跑通为准，这或许也可以认为是某种形式大于实质？比如在Github Apps的连通性检验中，不去直接跑项目路路径，而是自己去发请求，获取仓库内容；在PR并发的测试中只做了简单的进程并发，没有去走真实逻辑；还有LLM输出的半结构化检验，可能涉及到api的费用产生，直接没做。这也是我花来最大功夫和agent讨论的地方。
### TDD 强制在 AI 协作下的体感如何？它是阻碍还是放大器？

TDD 在 AI 协作里不是阻碍，反而更像放大器。AI 最大的问题并非不会写代码，而是在没有约束时很容易写出看起来合理、实际上错位的代码。这个项目里最典型的例子，是后期排查 `inspector -> fixer -> final findings` 断链问题时，模型把本应保留的问题错误标成 `reject`，导致后续 suggestion 完全丢失。如果没有前置回归测试，AI 很容易给出一个表面修复，却同时破坏另一条链路。测试用例把要修什么、什么不能坏表达得比自然语言更清楚，因此它在这里更像约束 AI 幻觉的护栏。

在传统开发里，很多人会把 TDD 看作一种理想化但偏慢的习惯；但在 AI 协作里，它的地位发生了变化。因为当代码由我自己逐行编写时，我对上下文、意图和局部实现有连续记忆；而当大量代码由 agent 分段生成时，这种连续记忆会被打断。此时测试不只是在验证结果，也承担了恢复上下文的职责。每次我把一个失败测试交给 agent，本质上是在告诉它：不需要理解整个系统，只需要把这个具体行为修到正确即可。这样做显著降低了 agent 在大范围内自由修改的风险。

当然，TDD 也不是没有代价。它会迫使我先想清楚一个行为应该如何被观察、如何被断言、失败时应该暴露什么信号。这在项目早期会显得更慢，尤其是在需求还不完全稳定的时候。但这种慢是值得的，因为它把原本会在后期爆发的混乱提前暴露了。我的体感是：没有 AI 的时候，TDD 主要帮助程序员保持严谨；有 AI 之后，它更多是在帮助程序员维持控制权。

### subagent-driven 工作流让智能体能自主跑多久不偏题？什么样的 task 拆解颗粒度最优？

subagent-driven workflow 的效果高度依赖 task 粒度。我这次最大的经验是：2 到 5 分钟、单一职责、能明确列出文件路径和验证命令的 task 最稳定；一旦 task 同时跨越数据模型、编排逻辑、前端展示和容器化，subagent 就很容易在局部做出貌似合理、整体却不一致的决定。subagent 并不擅长自行发现架构，它更擅长在既定边界内完成局部施工。因此 `PLAN.md` 的质量直接决定了 AI 能自主跑多远。

我觉得这背后反映了一个很现实的问题：subagent 的自主，更多体现为局部执行自主，而不是产品和架构层面的完全自主。它在补一个规则分析器、修一个 schema 校验漏洞、给现有页面换一套 dashboard 壳层这类任务上表现很好，因为这些任务边界清晰、输入输出明确；但它在重新定义某个模块应该承担什么职责时就明显不稳。这意味着，agentic workflow 并没有削弱架构设计的重要性，反而要求主导者把架构层判断做得更早、更显式。

此外，我这次也感受到 subagent 适合解决并行的局部问题，但不适合长时间连续持有一个模糊目标。它一旦脱离具体断言和文件边界，就容易开始补全它以为你想要的东西。因此对我来说，task 粒度最优并非越小越好，而是小到足以约束 agent，大到仍然保留一个可验证的业务意义。这和单纯的函数级拆分并不完全一样，它更强调任务能否在一次会话里闭环。

### SPEC 与 PLAN 的质量如何影响了后续实现质量？是否有过规约不清晰导致 subagent 跑偏的具体案例？
  
SPEC 与 PLAN 对后续实现质量的影响，比我一开始预想的还大。写得最成功的部分，是 `review_commit_sha`、stale run、双阶段 pipeline、JSON Schema 校验这些约束。正是因为这些东西在 SPEC 里被写死，后面的实现和排障才有稳定锚点。相反，写得不够好的地方主要体现在前端与部署：最初虽然写了本地可视化层，但没有把 Open Design 的 skill、界面风格和最终单容器部署策略说得足够明确，导致后来前端实现和文档补齐花了额外成本。这说明规约不清晰导致的跑偏，不一定表现为代码错误，也可能表现为方向模糊。

这里我最大的感受是，SPEC 和 PLAN 并不只是项目开始前要完成的文档作业，它们更像 agent 协作时代的共享控制面板。过去在纯人工团队里，很多模糊的地方可以靠口头沟通、临场判断和成员经验填补；但在和 agent 协作时，凡是没有被文档显式写出的假设，最后几乎都会变成成本。比如我最开始默认前端只是附属调试界面，但这个默认并没有被规约成它是一个 dashboard 型监控器、要最终托管进统一容器、要服务于 replay 和 trace 浏览。结果就是后面在前端和 README 上都发生了补课。

更重要的是，好的 SPEC 不只是功能列表，而是把“不允许发生什么”也讲清楚。这个项目里像 `review_commit_sha`、stale run 检查、`/review` 命令触发即使 Stage 1 零命中也必须走 Stage 2 fallback 这样的约束，都是在后续真实排障里起到决定性作用的。如果没有这些约束，很多 bug 最后都可能被解释成“模型不稳定”或者“线上环境偶发”，从而失去可修复性。

### 你设计的最有效的 prompt / context 策略是什么？为什么有效？
  
在 prompt / context 策略上，我认为最有效的是三点。第一，限制上下文，只给 diff、必要文件片段、候选问题和严格 schema，不给大而散的仓库背景，这能显著减少模型自由发挥。第二，把确认问题是否成立和生成最小修复建议拆成 `Inspector Agent` 与 `Fixer Agent` 两个角色，这让模型不至于在还没确认缺陷时就开始过度修补。第三，任何结构化输出都必须经过 schema 校验、业务校验和降级策略，不能直接信任模型返回值。这个项目后期能够稳定处理半结构化 payload、错误 verdict 和 suggestion 降级，本质上靠的不是模型更聪明，而是系统不再默认相信模型总是对的。

这里还有一个我后来越来越确定的经验：提示词工程真正有效的部分，往往不在于把 prompt 写得更聪明，而在于减少 prompt 需要承担的东西。也就是说，能用程序约束解决的问题，就不要交给 prompt。比如 commit SHA 绑定、文件路径合法性、行号范围、suggestion 锚点、模型输出结构，这些都应该由代码和 schema 把住，不应该寄希望于模型自行理解规范并严格遵守。一旦把这类硬约束从 prompt 中抽离出来，prompt 才能专注于解释问题和提出修复意图这类真正适合语言模型处理的任务。

我还意识到，prompt / context 策略和系统架构其实分不开。双 agent 之所以有效，并不只是因为 prompt 被分成两段，更重要的是系统接受了先判断是否存在问题，再决定是否生成 suggestion 这一流程拆分。换言之，好的 prompt 设计，本质上也是好的责任划分设计。

### Open Design 的 skill 库与设计系统在多大程度上消除了 AI 生成界面千篇一律的问题？
  
前端方面，Open Design 对减少 AI 生成界面千篇一律的问题确实有帮助，但它不是银弹。它最有价值的地方，在于帮我快速确定这不是一个营销页，而是一个 dashboard 型后台控制台，界面应该围绕指标卡片、历史列表、详情面板和操作侧栏组织信息。不过它主要解决风格和布局的起点问题，不能自动解决项目特有的信息组织问题。真正让页面有用的，仍然是我对审查历史、Agent trace、Replay 入口和 Findings 展示优先级的判断。

这一点其实和后端规约问题很像。Open Design 可以帮助我避免落入白底、几张卡片、几个按钮的默认 AI 界面模板，但它不会替我决定：什么信息应该在首页出现，什么信息应该进详情页，什么指标值得被提升为卡片，什么操作应该放在侧栏。这些都需要我基于项目目标自己做判断。也就是说，设计系统解决的是表现层一致性，不能代替产品层的信息架构。

不过从课程项目的角度看，Open Design 的价值仍然很明显。它让我能在较短时间内把一个能用但粗糙的调试页面，提升成一个更接近真实后台工具的界面。这种提升不只是审美层面的，也会反过来影响调试效率。一个能清楚区分列表、详情、trace、replay 和 findings 的界面，比单纯把 JSON 平铺在页面上，更有助于我发现系统链路真正断在哪里。

### 如果重做，你会改变哪些做法？

如果重做一次，我会更早做三件事。第一，更早冻结部署目标，把单容器、FastAPI 同时托管 API 和前端静态资源提前写进 SPEC，而不是在后期再统一。第二，更早对文档做人工审查，而不是直接让AGENT遵守，上下文拉长后基本会忘。第三，更早构造更贴近真实线上故障的端到端回归用例，例如容器重启后的队列恢复、远端 head 漂移、模型半结构化输出等。事实证明，项目后期最费时的工作不只是写功能，更在于把各种边界情形补齐到可以解释、可以复现。

### 你对 Superpowers 这套方法论的批判：它假设了什么？这些假设在你的项目里成立吗？
  
我对 Superpowers 这套方法论的批判是：它隐含假设了问题可以被事先良好规约、任务可以被稳定拆分、验证可以被自动化表达。这个假设在后端工程、接口编排、规则系统里大体成立，但在探索性设计、跨层重构、模糊需求澄清里并不总成立。流程本身也有成本，尤其是 worktree、日志、TDD、两阶段 review 同时执行时，人的注意力会被维持流程正确这件事大量占用。因此它更像一种高约束、高回报的方法，不适合被理解成免费提升效率的捷径。

我还想补充一点批判：Superpowers 其实默认纪律优先于灵活，但这在所有阶段都未必成立。在项目早期探索阶段，过早把任务切得太细、把流程卡得太死，有时会降低试错速度。比如某些问题本来只需要快速看一眼代码和日志就能得到方向，但如果机械地先写完整 task、再补日志、再维护多个 worktree，成本会变高。所以我认为 Superpowers 更适合项目从模糊走向收敛的中后期，不适合完全替代最初的探索性工作。

因此，方法论本身也需要结合情境使用。如果项目已经进入高复杂度、高并发、高回归风险阶段，那么强流程会非常有价值；如果项目还在问题定义高度不稳定的阶段，那么保留一定的快速试错空间也同样重要。真正成熟的工程实践，不在于盲目执行方法论，而在于知道什么时候该严格，什么时候该放松。

### 你对当前 AI4SE 工具与方法论的整体看法。

整体来看，我对当前 AI4SE 工具与方法论的看法是审慎乐观。AI 已经足以承担大量样板性实现、测试补全、接口对齐和回归修复工作，但前提是人类工程师愿意承担更高层的职责：定义边界、设置验证、做取舍、保留证据、处理例外。这个项目让我更确信，未来软件工程师的价值不会消失，而会更多转向设计一个让 AI 不容易犯大错的工程系统。

如果把这个判断再联系到现在常见的敏捷开发，我觉得 AI4SE 和敏捷之间不存在替代关系，它们更可能逐步融合。敏捷强调快速反馈、持续交付、迭代修正和对变化做响应，这些理念在 AI 协作时代反而更重要。因为 AI 可以极大提升单次实现速度，但它也会更快地产生偏差；如果没有短周期反馈、持续测试和可回滚的小步提交，AI 只会把错误放大得更快。从这个意义上说，TDD、回归测试、日志和工作流拆分，都可以被看作面向 AI 时代的敏捷实践强化版。

但 AI4SE 也会推动敏捷本身发生变化。过去敏捷团队讨论的重点，是如何让人类团队更快协作；现在还要进一步考虑如何让人类与多个 agent 一起协作。这意味着 backlog 的颗粒度、任务描述方式、完成定义、代码评审和测试策略都要调整。未来的敏捷迭代里，可能不只是站会讨论谁做什么，还要讨论哪些任务适合交给 agent、哪些约束必须先写进规约、哪些结果必须由人类最终判断。我认为这会让敏捷从一种团队协作方法，进一步演化成一种人类与智能体协作的治理方法。

总的来说，我对 AI4SE 的态度既不是盲目乐观，也不是悲观防御。我更倾向于认为，它正在把软件工程重新推回一个更强调基本功的位置。以前很多人觉得需求澄清、测试设计、文档约束、回归验证属于慢工作；但在 AI 大幅提高编码速度之后，真正决定系统质量的，恰恰是这些慢工作。谁能把这些工作做成系统，谁才能真正驾驭 AI，而不会被 AI 的高产出表象牵着走。
